# <Problem Number> — <Problem Title>

> Source: <e.g. LeetCode #5> · Difficulty: <Easy/Medium/Hard>
> **Categories:** <comma-separated DSA tags, e.g. String, Two Pointers, Dynamic Programming>

## 1. Problem Statement

<The original problem statement verbatim — description, examples, and constraints.>

**Examples**

```
Input:  <...>
Output: <...>
Explanation: <...>
```

**Constraints**

- <constraint 1>
- <constraint 2>

## 2. DSA Concepts Used

List the data structures and algorithmic techniques this problem exercises, so it can be categorised:

- **<e.g. Array / String / Hash Map / Stack / Queue / Linked List / Tree / Graph / Heap>** — <why it applies here>.
- **<e.g. Two Pointers / Sliding Window / DP / Greedy / Binary Search / Recursion>** — <why it applies here>.

## 3. Requirements / Targets

- **Expected time complexity:** <e.g. O(n)>
- **Expected space complexity:** <e.g. O(1)>
- **Input size:** <e.g. n ≤ 10^5>
- **Edge cases to handle:** <empty input, single element, duplicates, ...>

## 4. Possible Approaches

A short menu of ideas, ordered brute force → optimal:

1. **<Approach A — e.g. Brute Force>** — one-line idea.
2. **<Approach B — e.g. Better>** — one-line idea.
3. **<Approach C — e.g. Optimal>** — one-line idea.

## 5. Explanation of Each Approach

### Approach A — <name>
<How it works, the core insight, and trade-offs.>

### Approach B — <name>
<How it works, the core insight, and trade-offs.>

### Approach C — <name>
<How it works, the core insight, and trade-offs.>

## 6. Complexity of Each

| Approach | Time | Space | Notes |
|----------|------|-------|-------|
| A — <name> | O(?) | O(?) | <when to use / why rejected> |
| B — <name> | O(?) | O(?) | |
| C — <name> | O(?) | O(?) | ✅ recommended |

## 7. Implementation (Go)

```go
package main

import "fmt"

// <function> implements the recommended approach (<name>).
//
// Time:  O(?)
// Space: O(?)
func solve() {
	// ...
}

func main() {
	// verification against the examples
}
```

### Line-by-line walkthrough

Explain what every line (or tightly grouped block) of the code above does and why:

- `<line / block>` — <what it does and why it's there>.
- `<line / block>` — <what it does and why it's there>.
- ...

**Verification**

```
$ go run main.go
<expected output matching the examples>
```
